# 📋 ขั้นตอนต่อไปหลังจากสร้าง Wiki.js แล้ว

## ✅ สิ่งที่ทำเสร็จแล้ว

- [x] Wiki.js setup สำเร็จ
- [x] Login ได้
- [x] Wiki.js พร้อมใช้งาน

---

## 🎯 ขั้นตอนต่อไป

### Phase 1: ตั้งค่า Git Storage ใน Wiki.js

#### 1. สร้าง Test Git Repository (ถ้ายังไม่มี)

**Option A: สร้างใหม่ใน GitHub**
1. ไปที่ GitHub
2. สร้าง repository ใหม่ (เช่น `test-wiki-docs`)
3. ตั้งค่าเป็น Private หรือ Public ตามต้องการ

**Option B: ใช้ Repository ที่มีอยู่**
- ใช้ repository ที่มีอยู่แล้ว

#### 2. ตั้งค่า Git Storage ใน Wiki.js

1. **Login เข้า Wiki.js** (`http://localhost:3993`)

2. **ไปที่ Administration** → **Storage** → **Git**

3. **ตั้งค่า:**
   - **Authentication Type**: 
     - `SSH` (แนะนำ - ปลอดภัยกว่า)
     - หรือ `basic` (ง่ายกว่า)
   
   - **Repository URI**: 
     - SSH: `git@github.com:username/repo-name.git`
     - Basic: `https://github.com/username/repo-name.git`
   
   - **Branch**: `main` (หรือ branch ที่ต้องการ)
   
   - **SSH Private Key** (ถ้าใช้ SSH):
     - Mode: `contents`
     - วาง private key ในช่อง "SSH Private Key Contents"
   
   - **Username/Password** (ถ้าใช้ basic):
     - Username: GitHub username
     - Password: Personal Access Token (ไม่ใช่ password)
   
   - **Sync Direction**: 
     - `Bi-directional` (แนะนำ)
     - หรือ `Push to target` (ถ้าต้องการแค่ push)
   
   - **Sync Schedule**: `Every 5 minutes`

4. **Activate**: เปิดใช้งาน storage target

5. **Apply**: บันทึกการตั้งค่า

---

### Phase 2: ทดสอบ Git Storage

#### 1. สร้างเนื้อหาใน Wiki

1. สร้างหน้าใหม่ใน Wiki.js
2. เพิ่มเนื้อหา (Markdown)
3. Save

#### 2. ตรวจสอบว่า Sync ไป Git แล้ว

**ตรวจสอบใน GitHub:**
- ไปที่ repository
- ดูว่ามี commit ใหม่หรือไม่
- ดูว่ามีไฟล์ Markdown ใหม่หรือไม่

**หรือใช้คำสั่ง:**
```bash
# Clone repository (ถ้ายังไม่มี)
git clone <your-repo-url>
cd <repo-name>

# Pull ข้อมูลใหม่
git pull

# ดูไฟล์ที่มี
ls -la
```

---

### Phase 3: Import เข้า RAG System

#### Option A: ใช้ ChromaDB GitHub Sync (ง่ายที่สุด)

1. **ไปที่ ChromaDB Cloud** → **Sync** tab
2. **คลิก "Connect repository"**
3. **เลือก repository** ที่ใช้ใน Wiki.js
4. **ตั้งค่า sync options**
5. **ChromaDB จะ sync ข้อมูลอัตโนมัติ**

**ข้อดี:**
- ไม่ต้องเขียน script
- Sync อัตโนมัติ
- ใช้งานง่าย

---

#### Option B: สร้าง Import Script (ควบคุมได้เต็มที่)

**1. Clone Repository**

```bash
cd d:\rag-chatbot-project\docs
git clone <your-repo-url> test-wiki
```

**2. สร้าง Import Script**

```javascript
// scripts/import-docs-to-rag.js
// อ่านไฟล์ Markdown และ import เข้า ChromaDB
```

**3. รัน Import**

```bash
node scripts/import-docs-to-rag.js
```

---

## 📝 Testing Checklist

### Phase 1: Git Storage Setup
- [ ] สร้าง/เลือก Git repository
- [ ] ตั้งค่า Git Storage ใน Wiki.js
- [ ] Activate storage target
- [ ] ตรวจสอบว่า sync ทำงาน

### Phase 2: Content Creation
- [ ] สร้างหน้าใหม่ใน Wiki
- [ ] ตรวจสอบว่า commit ไป Git แล้ว
- [ ] ตรวจสอบใน GitHub repository

### Phase 3: RAG Integration
- [ ] ตั้งค่า ChromaDB GitHub Sync (Option A)
- [ ] หรือสร้าง import script (Option B)
- [ ] Import ข้อมูลเข้า ChromaDB
- [ ] ทดสอบค้นหาใน RAG system

---

## 🔄 Workflow สมบูรณ์

### Initial Setup:
```
1. Wiki.js → สร้างเนื้อหา
2. Git Storage → Push → GitHub
3. ChromaDB Sync → Import → ChromaDB
4. RAG System → พร้อมใช้งาน
```

### Auto Update:
```
1. Wiki.js → แก้ไขเนื้อหา
2. Git Storage → Auto push → GitHub
3. ChromaDB Sync → Auto sync → ChromaDB
4. RAG System → ข้อมูลอัปเดตอัตโนมัติ
```

---

## 🎯 สรุปขั้นตอนต่อไป

### สำหรับเพื่อน (Mac):

1. **ตั้งค่า Git Storage ใน Wiki.js**
   - ไปที่ Administration → Storage → Git
   - ตั้งค่า repository, credentials
   - Activate

2. **ทดสอบการสร้างเนื้อหา**
   - สร้างหน้าใหม่
   - ตรวจสอบว่า sync ไป Git แล้ว

3. **ตั้งค่า ChromaDB Sync** (ถ้าต้องการ)
   - ไปที่ ChromaDB Cloud → Sync
   - Connect repository

---

## 📚 เอกสารอ้างอิง

- `WIKI_TEST_SETUP.md` - คู่มือการตั้งค่า Wiki.js
- `SETUP_FOR_MAC.md` - คู่มือสำหรับ Mac
- `HOW_TO_RUN.md` - คู่มือการรันระบบ

---

## 🆘 ถ้ามีปัญหา

### Git Storage ไม่ทำงาน
- ตรวจสอบ SSH key หรือ credentials
- ตรวจสอบ repository URL
- ตรวจสอบ network connection

### Sync ไม่ทำงาน
- ตรวจสอบ sync schedule
- ตรวจสอบ logs ใน Wiki.js
- ตรวจสอบใน GitHub repository

---

## ✅ พร้อมสำหรับขั้นตอนต่อไป!

เมื่อตั้งค่า Git Storage แล้ว:
1. ทดสอบการสร้างเนื้อหา
2. ตรวจสอบว่า sync ไป Git แล้ว
3. Import เข้า RAG system
