# Ollama Configuration
# Ollama อยู่ที่ Mac เครื่องอื่น (192.168.11.71)
OLLAMA_URL=http://192.168.11.71:11434

# ChromaDB Configuration
# สำหรับ ChromaDB Cloud: ใช้ CloudClient (แนะนำ)
CHROMA_API_KEY=your-chromadb-api-key-here
CHROMA_TENANT=your-chromadb-tenant-here
CHROMA_DATABASE=your-chromadb-database-here

# สำหรับ Local ChromaDB: ใช้ URL (ถ้าไม่ใช้ Cloud)
# CHROMA_URL=http://localhost:8000

CHROMA_COLLECTION_NAME=rag_documents

# Embedding Model Configuration
# ต้องดาวน์โหลดบน Mac: ollama pull nomic-embed-text
EMBEDDING_MODEL=nomic-embed-text

# LLM Model for Generation
# ใช้ llama3.2 เพราะมีอยู่แล้วบน Mac
LLM_MODEL=llama3.2
